---
- name: provision files, templates and content
  host: all
  become: yes
  tasks:
    - name: Banner file /etc/motd
      copy: 
        content: "This {{ansible_distribution}} is managed by Ansible. \n"
        dest: /etc/motd